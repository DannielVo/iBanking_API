import uvicorn 
from fastapi import FastAPI,HTTPException, Request
from pydantic import BaseModel, EmailStr, Field
from fastapi.responses import JSONResponse
from typing import Annotated
import logging
import pyodbc

máy nào dùng làm sever thì chỉnh database ở máy đó
Do hệ thống Ibanking được giả sử là thiết kế cho sinh viên cuả TDTU cho nên để đăng nhập được vào hệ thống này thì phải là sinh viên của TDTU. Mà là sinh viên của trường thì sẽ có phần học phí nợ. Cho nên phần debt sẽ nằm ở customers. 